---
title: "실행 가능한 연구로: 재현 가능한 프로젝트와 사례 검토"
week: 3
order: 0
---

2주차에 세운 질문이 연구로 이어지려면 자료를 열 수 있어야 하고, 작은 분석을 실행할 수
있어야 하며, 다른 팀원이 그 결과를 확인할 수 있어야 한다. 이번 주의 목표는
**주제의 실현가능성(feasibility)을 증거로 판단하는 것**이다. 폴더와 코드를 정리하는
일도 이 판단에 포함된다. 데이터 처리 과정을 추적할 수 있어야 주제의 전제가 맞는지
검사할 수 있기 때문이다.

**지정 읽기**:

- Yu & Barter, [Chapter 3 — Setting Up Your Data Science Project](https://vdsbook.com/03-code_reproducibility) 전체.
- 양경주 (2024), [현실적인 데이터 과학의 사례 연구](https://dcollection.snu.ac.kr/common/orgView/000000184901).
  특히 §3.1–3.4(본문 21–28쪽), §3.5의 지표 정의(34–35쪽), §3.6–3.7(60–73쪽),
  §4.2(75–76쪽)를 읽는다. §3.5의 상세 코드는 자료가 정제되는 경로를 따라가며 참고한다.
  페이지는 논문에 인쇄된 번호다. 배포 PDF에서는 4를 더하면 해당 파일 페이지가 된다.

<Callout type="note">
이 노트는 초안이며 활동·산출물 양식은 운영 제안이다. 사례의 사실과 수치는 논문의
보고 내용이고, “검토 질문”과 가상 계산은 수업에서 추가한 분석이다. 원자료와 저자의
전체 코드를 재실행해 논문 결과를 재현한 것은 아니다.
</Callout>

## 1. 폴더가 있다는 것과 실행할 수 있다는 것

Chapter 3은 코드 구조, 원자료 보존, 재현성, 협업 도구를 연결한다.[^setup]
이를 우리 팀에 적용하는 간단한 검사는 다음과 같다. “분석을 작성하지 않은 팀원이
새 폴더에서 안내 문서만 읽고 핵심 표 하나를 만들 수 있는가?”

재실행 중 `final2.csv`를 구두로 전달하거나 노트북의 특정 셀만 먼저 실행해야 한다면,
그 과정에는 아직 문서화되지 않은 의존성이 있다. README에 빠진 단계를 추가하고,
그 단계를 코드로 만들 수 있는지 검토한다.

### 1.1 팀원이 따라갈 수 있는 실행 경로

아래는 이번 수업의 Python 프로젝트 예시다. 디렉터리 이름은 팀이 바꿔도 된다.
중요한 것은 원자료에서 결과까지의 경로와 판단의 이유를 추적하는 것이다. Barter의
워크플로 해설도 원자료 보존과 실행 가능한 코드·탐색 기록의 구분을 강조한다.[^workflow]

```text
project/
  README.md                 # 질문, 데이터 확보 방법, 실행 순서, 예상 출력
  requirements.txt          # 실행에 사용한 패키지 버전
  .gitignore                # 공개하지 않을 데이터와 개인 환경 파일
  data/
    raw/                    # 내려받은 원본, 직접 수정하지 않음
  docs/
    data_dictionary.md      # 단위, 시점, 결측, 범주 정의
    decisions.md            # 선택 이유, 대안, 변경 이력
  src/
    prepare.py              # 원자료를 분석 가능한 형태로 변환
    evaluate.py             # 기준 방법과 평가 지표 계산
  notebooks/
    01_pilot.ipynb          # 탐색 과정과 결과 해석
  outputs/
    pilot_metrics.csv       # 코드로 다시 생성할 핵심 표
```

이 트리는 학생 프로젝트를 위한 예시다. 이 강의 자료 저장소의 디렉터리 구조와는 다르다.
`data/raw/`에 파일이 있다고 Git에도 올려야 하는 것은 아니다. 사용 조건이나 개인정보
때문에 공유할 수 없는 자료는 저장소에 넣지 않고 적법한 접근 절차를 문서화한다.
동료가 권한을 갖고 있는지와 코드가 재실행되는지는 따로 확인한다.

README에는 “분석 노트북 실행”보다 자세한 안내가 필요하다. 프로젝트 루트에서
실행하는지, 어떤 파일이 입력인지, 어떤 패키지 버전이 필요한지, 결과 파일과 예상
행 수가 무엇인지 적는다. 실제로 존재하지 않는 명령을 안내용으로만 적어 두지 않는다.

가공 자료를 저장하면 계산 시간을 줄일 수 있지만 원자료나 정제 규칙이 바뀐 뒤 낡은
파일을 사용할 위험이 있다. 교재는 분석 시 원자료를 다시 정제하는 방식을 권장하고,
정제가 오래 걸려 중간 결과를 저장할 때에는 생성 과정과 갱신을 관리하도록 한다.[^setup]
우리 팀이 중간 파일을 저장한다면 입력 버전·정제 설정·생성 코드를 함께 기록한다.

### 1.2 선택을 함수의 입력으로 드러내기

가상 식당 자료에서 결측이 있는 날을 모두 제외할지, 기록이 충분한 구간만 사용할지는
분석자의 선택이다. 두 선택을 검사하려고 노트북 전체를 복사하면 공통 부분까지
서로 달라질 수 있다. 공통 계산은 함수로 묶고 비교할 선택은 인수나 설정에 드러낸다.

함수에는 “무엇이 들어오며 무엇이 나오는가”를 적는다. 비율을 계산한다면 입력이
0–1인지 0–100인지, 결측이 오면 어떻게 처리하는지, 빈 입력을 허용하는지 명시한다.
같은 이름의 결과 파일을 덮어쓰는 대신 어떤 설정에서 나온 값인지 결과에 남긴다.

### 1.3 협업에서 Git과 검토가 맡는 일

Git은 파일 변경 이력을, GitHub 같은 서비스는 원격 공유와 리뷰 경로를 제공한다.
원자료의 뜻과 분석 선택의 타당성은 도구가 판단해 주지 않는다.[^setup]
팀은 다음처럼 하나의 작은 변경을 끝까지 넘겨 본다.

1. “대기 시간의 단위와 결측 코드를 확인한다”처럼 완료 조건이 있는 작업을 등록한다.
2. 담당자가 문서나 코드의 변경과 이유를 남긴다.
3. 검토자는 입력 예시 하나로 결과를 확인하고, 해석이 맞는지 질문한다.
4. 합의한 변경을 반영한 뒤 검토자가 다시 실행한다.

패키지 버전과 난수 seed를 기록해도 실행 환경, 입력 파일, 숨은 실행 순서가 다르면
결과가 달라질 수 있다. 노트북은 커널을 재시작한 뒤 처음부터 실행한다. 오차 허용이
필요한 계산이라면 값이 어느 범위에서 같아야 하는지도 정한다.

## 2. 사례를 읽는 출발점: 무엇을 비교했는가

양경주의 사례는 제22대 국회의원 선거를 대상으로 전국 단위 여론조사와 비례대표
선거 결과를 비교한다. 논문은 선택한 기간의 여론조사 **234건**을 분석하고, 실제
득표율을 이용해 각 조사 결과를 평가한다. 조사 시작일은 2023년 10월 29일부터,
종료일은 2024년 4월 3일까지라는 범위를 사용한다.[^case-data]

이 사례를 볼 때 “선거를 잘 맞혔는가”를 구체적인 세 부분으로 나눈다.

| 요소 | 논문에서 다룬 대상 | 읽을 때 구분할 것 |
|---|---|---|
| 관측 단위 | 전국 단위 여론조사 1건 | 유권자 개인 1명이 아님 |
| 비교 대상 | 정당 지지율과 비례대표 선거 득표율 | 지역구 당선자·의석수와 다름 |
| 분석의 질문 | 정의한 기준을 충족한 조사들의 특징과 그 특징의 안정성 | 조사 방법의 인과적 효과를 직접 식별하는 설계와 다름 |

논문 §3.3은 여론조사와 개표 결과의 원파일 출처 및 내려받은 날짜를 밝히고, §3.4는
정제·전처리·분석 문서와 함수를 구분한다. 이 부분을 Chapter 3과 연결해 읽는다.
파일명이 멋진지를 평가하기보다 “최종 표의 숫자를 어떤 원파일과 판단까지 거슬러
올라갈 수 있는가”를 확인한다.[^case-data]

### 2.1 전처리가 비교 대상의 뜻을 바꾼다

여론조사의 정당 범주와 선거 결과의 정당 범주는 그대로 일치하지 않는다. 논문은 정당
범주를 대응시키고, 지지정당 없음·모름/무응답 등을 처리해 비교 가능한 형태를 만든다.
최종 지표 계산에서는 **8개 정당 범주**를 사용한다.[^case-metric]
원자료 화면은 논문 그림 3.2–3.8(29–33쪽), 비교 형태는 표 3.1(35쪽)에서 확인한다.
이 그림과 표는 원문에서 읽고, 노트에는 아래의 별도 가상 예시로 계산을 연습한다.

검토 질문은 “열 이름을 맞췄는가”에서 끝나지 않는다. 평소 지지하는 정당과 실제
비례대표 투표 의향이 같은 개념인지, 무응답 열을 삭제하는 것과 남은 비율의 분모를 다시 정하는 것이 어떻게
다른지도 살펴야 한다. 논문에서 실제 수행한 변환과 우리가 검토할 대안을 구분한다.
범주 대응과 분모 선택은 substantive judgment, 곧 내용에 관한 판단이다. 이는 이 사례를 읽으며 제기하는 검토 사항이지 논문이 별도로 검증한
결과라는 뜻은 아니다.

**우리 팀에 적용하기.** 원자료의 변수 하나를 골라 최종 분석 변수까지의 변환을
설명한다. 예를 들어 “결제 시각 → 30분 구간 → 구간별 주문 수”의 각 화살표에서
사라지는 정보와 새로 들어가는 가정을 적는다.

## 3. 포함률을 손으로 계산하고 해석하기

논문은 조사별 정당 지지율 주변의 구간에 실제 득표율이 들어오는 범주의 비율을
‘포함률’로 정의한다. 논문 식 (3.2)를 우리 표기로 쓰면 다음과 같다.[^case-metric]

$$
C_i=\frac{1}{m}\sum_{j=1}^{m}\mathbf{1}\bigl(|r_{ij}-p_j|\le e_i\bigr).
$$

$r_{ij}$는 조사 $i$의 범주 $j$ 지지율(%), $p_j$는 대응하는 실제 득표율(%),
$e_i$는 조사에 보고된 표본오차(%p), $m$은 비교 범주 수다. $\mathbf{1}$은 조건이
맞으면 1, 아니면 0을 주는 indicator다. $e_i$를 표준오차(standard error)로
해석하지 않는다. 여기서는 논문이 사용한 표본오차의 폭을 가리킨다.

다음은 정당명이 없는 **4개 범주의 가상 자료**다. 표본오차는 3%p라고 하자.

| 범주 | 조사 지지율 | 실제 득표율 | 절대 차이 | 3%p 이내인가 |
|---|---|---|---|---|
| A | 40% | 42% | 2%p | 예 |
| B | 30% | 27% | 3%p | 예 |
| C | 20% | 19% | 1%p | 예 |
| D | 10% | 12% | 2%p | 예 |

경계를 포함하는 식에 따르면 $C_i=4/4=1$이다. 폭을 2%p로 줄이면 B가 빠져
$C_i=3/4=0.75$가 된다. 예측값이 같아도 구간 폭에 따라 점수가 달라진다.
따라서 포함률만 보고 점예측 오차나 조사 품질 전체를 판단하지 않는다.

<Callout type="warning">
여기의 포함률은 한 선거 결과와 여러 정당 범주를 비교한 경험적 지표다. 반복 표집에서의
95% 신뢰구간 coverage와 같은 양이 아니다. 조사에 보고된 하나의 표본오차를 범주마다
적용하는 판단도 검토 대상이다. 지표의 이름만 보고 통계적 보장이 있다고 해석하지 않는다.
</Callout>

### 3.1 논문의 수치가 말하는 범위

논문은 포함률 0.7 이상을 기준으로 삼았다. 범주가 8개이므로 가능한 값은 0.125씩
증가하며, 0.7 이상은 적어도 6개 범주가 포함된다는 뜻이다. 표 3.2(61쪽)의 빈도는
0.25가 1건, 0.375가 7건, 0.5가 104건, 0.625가 113건, 0.75가 9건으로 합계
234건이다. 따라서 이 기준을 충족한 조사는 9건이다.[^case-results]

그중 무선 ARS(automatic response system)와 무선 RDD(random digit dialing)의
조합은 7건이었다. $7/9\approx77.8\%$는 **기준을 충족한 조사 중 그 조합의 비율**이다.
그 조합으로 진행한 모든 조사의 성공률을 계산하려면 분모에 그 조합의 전체 조사 수가
필요하다.[^case-results]

가상으로 기준을 충족한 10건 중 방법 A가 8건, B가 2건이라고 하자. 전체 시행이
A 80건, B 4건이었다면 방법별 충족률은 A가 10%, B가 50%다. 이 예시는 구성 비율과
방법별 충족률이 다른 질문에 답한다는 것을 보여 준다. 실제 사례의 방법별 성공률을
계산한 값은 아니다.

또한 포함률이 높다는 기준을 0.70에서 0.75로 바꾸어도 8개 범주에서는 같은 조사들이
선택된다. threshold에 대한 민감도를 보려면 지표가 실제로 변할 수 있는 경계를
고려해야 한다. “기준을 바꿨는데 같았다”는 말만으로 충분한 검사가 되지 않을 수 있다.

### 3.2 수식과 코드가 같은 규칙인가

논문 식 (3.2)는 경계를 포함하는 부등호를 사용하지만, 본문 61쪽의 구현은 경계를
제외하는 비교를 사용한다. 실제 결과에 차이가 생기는지는 원자료의 경계값을 검사해야
한다. 이번 수업에서는 경계를 포함하는 수식을 기준으로 다음 코드를 직접 작성했다.
논문 코드를 복사하거나 번역한 것이 아니다.

```python
def inclusion_score(poll, result, margin):
    """백분율 값 두 목록과 %p 단위의 비음수 폭을 받는다."""
    if len(poll) != len(result) or not poll:
        raise ValueError("비어 있지 않은 같은 길이의 목록이 필요합니다.")
    if margin < 0:
        raise ValueError("표본오차의 폭은 음수일 수 없습니다.")
    hits = sum(abs(a - b) <= margin for a, b in zip(poll, result))
    return hits / len(poll)


poll = [40, 30, 20, 10]
result = [42, 27, 19, 12]
print(inclusion_score(poll, result, 3))  # 1.0
print(inclusion_score(poll, result, 2))  # 0.75
```

이 코드는 결측 없는 유한한 숫자 목록을 가정한 계산 연습이다. 실제 분석에서는 범주
대응, 결측, 단위, 값의 유효성 등을 먼저 검증해야 한다. B 범주의 차이가 정확히 3인
예시는 경계 조건을 확인하는 데 유용하다. 작은 손계산을 코드와 대조하는 습관이
재현 가능한 프로젝트의 첫 검사다.

## 4. 섭동을 읽는 법: 무엇을 보존하고 무엇을 바꿨는가

논문은 조사 방법·표본추출 틀의 조합, 표본수, 접촉률, 응답률, 표본오차를 기준으로
그룹을 나누는 다섯 가지 실험을 수행한다. 각 실험에서는 해당 그룹 안에서 정당별
지지율 열의 값을 따로 섞고 포함률을 다시 계산한다. 각각 1,000회 반복하며 난수
seed도 명시한다.[^case-stability]

첫 실험에서 논문이 보고한 0.956은 단순한 “정확도 95.6%”가 아니다. 한 반복에서
포함률 기준을 충족한 조사들을 뽑고, 그중 무선 ARS·무선 RDD 조합이 차지한 비율을
구한다. 그 비율이 0.5 이상인 반복의 비율이 0.956이다. 여기서는 구현의 `>= 0.5`를
따랐으며, 본문의 일부 문장에는 “0.5를 넘는”이라고 적혀 있어 경계에 주의한다.
기준 충족 조사가 없는 반복에는 구현상 비율 0을 부여한다.[^case-stability]

### 4.1 검토 질문: 열을 따로 섞어도 현실적인 조사인가

다음은 두 범주만 있는 가상 조사다.

| 원래 행 | A 지지율 | B 지지율 | 합계 |
|---|---|---|---|
| 조사 1 | 60 | 40 | 100 |
| 조사 2 | 30 | 70 | 100 |

A 열만 서로 바꾸면 두 행은 `(30, 40)`과 `(60, 70)`이 된다. 합계가 70과 130이므로
두 범주가 전체를 이룬다는 조건을 만족하지 않는다. 열별 셔플은 그룹 내 각 열의 값
목록을 유지하지만, **한 행에서 함께 관측된 값들의 관계**는 깨뜨릴 수 있다.

이것은 논문의 원자료를 다시 분석한 결과가 아니라 섭동 방식의 성질을 보이는 계산이다.
이 성질이 연구 목적에 맞는지 검토해야 한다. 예를 들어 행 전체를 재표집하면 정당 간
구성은 보존하지만 다른 종류의 변동을 검사한다. 시간 구간이나 조사기관을 통째로
남겨 두는 검사는 또 다른 불확실성을 다룬다. 하나를 무조건 더 좋은 방법으로 정하기
전에 어느 대안 현실을 모사하려는지 설명한다.

### 4.2 검토 질문: 다른 판단에도 결과가 유지되는가

논문 §4.2는 연속 변수를 두 범주로 나누는 기준값의 변화와 다른 정량 지표의 사용을
후속 검토 과제로 남긴다.[^case-limits] 이를 출발점으로 우리 팀은 다음을 토론한다.

- 같은 실제 득표율과 비교하더라도 오래전 조사와 직전 조사를 함께 평가할 근거는 무엇인가?
- 지지정당 없음과 무응답의 처리, 정당 범주 대응을 바꾸면 결과가 얼마나 달라지는가?
- 구간 포함 여부와 함께 절대오차를 보면 평가가 어떻게 달라지는가?
- 같은 기관의 반복 조사를 독립적인 새 증거처럼 세고 있지 않은가?

논문은 발표 일자가 자료에 없어서 조사 일자를 사용했다고 설명한다.[^case-data]
실제 공개 시점에 사용할 의사결정을 연구하려면 “조사를 마친 날”과 “사용자가 결과를
볼 수 있었던 날”을 구분해야 한다. 이는 2주차 식당 예시의 입력 이용 가능 시점과
같은 문제다.

이 사례에서 가져올 습관은 질문·지표·정제·섭동·해석을 연결해 공개하는 것이다.
어떤 연구를 참고하더라도 사용한 지표와 선택을 우리 문제에 맞게 다시 정당화해야 한다.

## 5. 우리 주제의 feasibility review

이제 팀의 문제 정의와 실제 표본을 나란히 놓는다. “데이터가 많다” 대신 필요한
변수가 있는지, “분석할 수 있다” 대신 작은 계산을 실행했는지 보여 준다.

### 5.1 최소 실행 실험

완성 모델 전에 다음 경로를 한 번 통과한다.

1. **표본 열기.** 출처와 내려받은 시점, 접근 조건, 파일 크기를 기록한다. 합성 자료로
   코드만 점검했다면 실제 자료 접근을 확인한 것으로 세지 않는다.
2. **질문에 연결하기.** 한 행의 단위, 입력, 정답, 시점을 지정한다. 결합 자료가 있다면
   키의 중복과 결합되지 않는 행을 확인한다.
3. **작은 baseline 계산하기.** 예측은 간단한 기준 방법의 지표를, 기술·비교 연구는
   비교할 요약 통계와 확인 방법을 만든다. 표본 결과를 전체 성능으로 보고하지 않는다.
4. **동료 재실행.** 다른 팀원이 문서대로 실행하고 결과, 소요 시간, 막힌 단계를 남긴다.
5. **판단 갱신.** 가장 불확실한 전제의 상태를 확인하고 계속·축소·전환을 결정한다.

가상 식당 팀이 결제 시각만 있는 파일을 확보했다면 “대기 시간 예측”의 정답은 아직
없다. “주문량 예측”으로 범위를 바꿀 수 있지만, 사용자의 어떤 판단을 돕는지도 함께
바꿔 써야 한다. 좋은 점수를 낼 수 있다는 이유만으로 목적을 조용히 바꾸지 않는다.

### 5.2 결정은 점수 합계보다 근거로

| 판단 | 필요한 증거 | 예시 |
|---|---|---|
| 계속 | 표본·정답·평가 경로가 있고 첫 실행이 가능 | 이후 주간으로 baseline을 평가했다 |
| 조건부 계속 | 핵심 장애에 확인 기한과 대안이 있다 | 기록 시각의 뜻을 담당자에게 확인 중이다 |
| 범위 축소 | 좁은 질문에는 자료와 검증 근거가 있다 | 전체 식당 대신 기록이 있는 한 식당을 다룬다 |
| 전환 | 핵심 변수를 얻을 경로가 없거나 검증이 성립하지 않는다 | 대기 시간 기록을 확보할 수 없다 |

흥미·접근성·기여도를 점수로 매길 수는 있지만, 정답 부재나 이용 불가를 다른 항목의
높은 점수로 상쇄하지 않는다. 연구의 기여도 함께 확인한다. 가장 가까운 선행연구와
비교해 대상·자료·방법·검증 중 무엇을 새로 밝히는지 한 문장으로 쓴다. 단순한 알고리즘
교체라면 그 교체가 어떤 미해결 질문에 답하는지 설명한다.

## 6. 수업 활동과 제안 발표 준비

180분 운영 예시는 실행 구조 점검 25분, 사례 읽기와 계산 45분, 휴식 10분, 팀별 pilot
50분, 동료 검토 35분, 결정 정리 15분이다. 당일에 확보할 수 없는 자료는 접근 상태와
다음 확인 행동을 명시한다.

팀은 4주차 M1(milestone 1) 제안 발표를 위해 다음 증거 묶음을 준비한다. 분량을
늘리는 것이 목적은 아니다. 핵심 주장을 뒷받침하는 파일이나 실행 기록에 연결한다.

| 증거 | 포함할 내용 |
|---|---|
| 수정한 문제 정의 | 대상·단위·시점·활용, 기존 질문에서 바뀐 이유 |
| 데이터 확인 | 실제 표본의 구조, 변수 정의, 접근·공유 조건, 아직 없는 정보 |
| 관련 연구와 기여 | 가장 가까운 연구와 남은 질문, 우리 프로젝트의 차이 |
| 실행 기록 | baseline 또는 첫 요약 표, 환경, 시간, 동료 재실행 결과 |
| 평가·안정성 계획 | 무엇을 남겨 평가하고 어떤 타당한 선택을 바꿀 것인가 |
| 결정과 일정 | 계속·조건부 계속·축소·전환의 근거, 다음 행동과 담당자 |

### 이해 확인

1. 기준 충족 조사 9건 중 특정 조합이 7건이다. 그 조합의 성공률이 77.8%인가?
2. 지지율 열을 따로 섞는 섭동은 무엇을 보존하고 무엇을 깨뜨릴 수 있는가?
3. 폴더 구조와 seed가 같으면 다른 팀원의 재실행을 생략해도 되는가?
4. 실제 표본에 정답 변수가 없다. 다음 주까지 무엇을 확인하고 어떤 대안을 준비할 것인가?

<Details summary="토론 후 확인할 해설">

1. 기준 충족 집합의 구성 비율이다. 방법별 성공률의 분모는 해당 조합의 전체 조사 수다.
2. 그룹 안의 열별 값 목록은 보존하지만 정당 간 관계와 행별 합계는 깨뜨릴 수 있다.
   이 변형이 질문에 적합한지 따로 설명해야 한다.
3. 입력 버전, 환경, 실행 순서, 접근 권한이 빠졌을 수 있다. 동료가 직접 실행해야 그
   장애를 확인할 수 있다.
4. 정답의 실제 확보 경로와 기한을 확인한다. 확보할 수 없으면 관측 가능한 결과를
   대상으로 질문과 활용 목적을 함께 바꾸고 그 이유를 기록한다.

</Details>

[^setup]: Yu, B., & Barter, R. L. (2024). [Setting Up Your Data Science Project](https://vdsbook.com/03-code_reproducibility). *Veridical Data Science*, MIT Press. — Chapter 3, §3.1의 함수·코드 검토, §3.2의 파일 구조와 원자료 보존, §3.3의 재현성, §3.4의 협업. 웹판 접근: 2026-09-10.
[^workflow]: Barter, R. L. (2019). [A quick guide to developing a reproducible and consistent data science workflow](https://rebeccabarter.com/blog/2019-03-07_reproducible_pipeline). *Rebecca Barter*. — 3월 23일 게시, Project Structure의 원자료·실행 코드·탐색 기록 구분. 해설 자료. 접근: 2026-09-10.
[^case-data]: 양경주. (2024). [현실적인 데이터 과학의 사례 연구 (Case study on Veridical data science)](https://dcollection.snu.ac.kr/common/orgView/000000184901). *서울대학교 대학원 통계학과 이학석사 학위논문*. — §3.1–3.4, 본문 21–28쪽, 특히 23쪽의 기간·234건·발표 일자 제약과 27–28쪽의 출처·파일 구조. 제공된 원문 PDF 대조: 2026-09-10.
[^case-metric]: 양경주. (2024). [현실적인 데이터 과학의 사례 연구](https://dcollection.snu.ac.kr/common/orgView/000000184901). *서울대학교 석사학위논문*. — §3.5, 본문 29–35쪽, 그림 3.2–3.8, 표 3.1 및 식 (3.2)의 범주 대응·포함률 정의; 61쪽 구현의 경계 비교와 대조.
[^case-results]: 양경주. (2024). [현실적인 데이터 과학의 사례 연구](https://dcollection.snu.ac.kr/common/orgView/000000184901). *서울대학교 석사학위논문*. — §3.6.2, 본문 61–63쪽, 표 3.2–3.5의 빈도와 방법·표본추출 틀 조합; §3.7, 72–73쪽의 결과 해석.
[^case-stability]: 양경주. (2024). [현실적인 데이터 과학의 사례 연구](https://dcollection.snu.ac.kr/common/orgView/000000184901). *서울대학교 석사학위논문*. — §3.6.2, 본문 63–71쪽의 그룹 내 열별 섭동, 반복·seed·빈 집합 처리·비율 판정 코드; §3.7, 73쪽의 결과.
[^case-limits]: 양경주. (2024). [현실적인 데이터 과학의 사례 연구](https://dcollection.snu.ac.kr/common/orgView/000000184901). *서울대학교 석사학위논문*. — §4.2, 본문 75–76쪽의 범주화 기준값 및 대안 지표에 관한 후속 과제. 원문 PDF의 이용허락은 CC BY-NC-ND 2.0 KR; 이 노트에는 원문 그림·코드를 재배포하지 않고 독자적인 계산과 비평을 제시한다.
